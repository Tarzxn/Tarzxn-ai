const state={model:'openrouter/free',history:[],apiKey:sessionStorage.getItem('forgeOpenRouterKey')||''};const $=s=>document.querySelector(s);const convo=$('#conversation');
function escapeHtml(t){return String(t).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function add(role,html){const el=document.createElement('div');el.className=`message ${role}`;el.innerHTML=html;convo.append(el);el.scrollIntoView({behavior:'smooth',block:'end'});return el}
async function loadModels(){try{const res=await fetch('/api/models');const models=await res.json();$('#models').innerHTML=models.map(m=>`<button class="model-choice" data-id="${escapeHtml(m.id)}" data-name="${escapeHtml(m.name)}"><strong>${escapeHtml(m.name)}</strong><small>${escapeHtml(m.family)} · ${escapeHtml(m.tag)}</small></button>`).join('');document.querySelectorAll('.model-choice').forEach(b=>b.onclick=()=>{state.model=b.dataset.id;$('#modelName').textContent=b.dataset.name;$('#modelMenu').classList.remove('open')})}catch(e){$('#models').textContent='Model list unavailable.'}}
$('#modelButton').onclick=()=>$('#modelMenu').classList.toggle('open');$('#newChat').onclick=()=>{state.history=[];convo.innerHTML=$('#hero').outerHTML};document.querySelectorAll('.suggestions button').forEach(b=>b.onclick=()=>{$('#prompt').value=b.textContent;$('#prompt').focus()});
$('#keyButton').onclick=()=>{$('#apiKey').value=state.apiKey;$('#keyModal').classList.add('open');$('#apiKey').focus()};$('#closeKey').onclick=()=>$('#keyModal').classList.remove('open');$('#saveKey').onclick=()=>{state.apiKey=$('#apiKey').value.trim();if(state.apiKey)sessionStorage.setItem('forgeOpenRouterKey',state.apiKey);else sessionStorage.removeItem('forgeOpenRouterKey');$('#keyModal').classList.remove('open')};
$('#composer').onsubmit=async e=>{e.preventDefault();const prompt=$('#prompt').value.trim();if(!prompt)return;if(!state.apiKey){$('#keyButton').click();return}$('#hero')?.remove();add('user',escapeHtml(prompt));$('#prompt').value='';const pending=add('assistant typing','Forge is creating your workspace…');$('#send').disabled=true;try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({prompt,model:state.model,history:state.history,apiKey:state.apiKey})});const data=await r.json();if(!r.ok)throw Error(data.error||'Request failed');pending.classList.remove('typing');pending.innerHTML=`<div>${escapeHtml(data.reply).replace(/\n/g,'<br>')}</div><div class="artifact"><strong>${data.files.length} file${data.files.length===1?'':'s'} created</strong><ul>${data.files.map(f=>`<li>${escapeHtml(f.path)} <small>${f.bytes} bytes</small></li>`).join('')}</ul><a class="download" href="/api/download/${data.workspace}">Download ZIP ↓</a></div>`;state.history.push({role:'user',content:prompt},{role:'assistant',content:data.reply})}catch(err){pending.innerHTML=`<span style="color:#ff9b8a">${escapeHtml(err.message)}</span>`}finally{$('#send').disabled=false}};loadModels();
